{
  "name": "procont-cloud-core",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/web",
    "packages/shared"
  ],
  "scripts": {
    "lint": "npm run lint -w apps/web && npm run lint -w packages/shared",
    "build": "dotnet build ProcontCloudCore.sln && npm run build -w packages/shared && npm run build -w apps/web",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,mjs,json,css,md,yml,yaml}\""
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "typescript": "^5.8.3"
  }
}
